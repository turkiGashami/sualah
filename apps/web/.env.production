# Public build-time config, baked into the client bundle by `next build`.
# The GA4 Measurement ID is public (it ships in the page anyway) — safe to commit.
# Read on both Railpack and Dockerfile builds; CranL env vars can still override.
NEXT_PUBLIC_GA_ID=G-PVVBKKL4S1
